<Window x:Class="SnipAgent.App.Overlays.AiAnswerOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Width="760" Height="520"
        MinWidth="480" MinHeight="320">
    <Grid>
        <Grid.Resources>
            <Style x:Key="ResizeThumbStyle" TargetType="{x:Type Thumb}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="Panel.ZIndex" Value="10" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Border Background="{TemplateBinding Background}" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Border CornerRadius="12" Padding="20">
        <Border.Background>
            <SolidColorBrush x:Name="PanelBackgroundBrush" Color="#1E1E1E" Opacity="0.93" />
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left"
                            VerticalAlignment="Center">
                    <TextBlock Text="AI Skill" Foreground="#AAAAAA" FontSize="11"
                               VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox x:Name="AiSkillsComboBox" Width="280" MinHeight="28"
                              HorizontalAlignment="Left"
                              Background="#661E1E1E"
                              BorderBrush="#66FFFFFF"
                              Foreground="White">
                    <ComboBox.Resources>
                        <Style TargetType="{x:Type ComboBoxItem}">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="Padding" Value="10,6" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                        <Border x:Name="ItemBorder"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="3">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#333333" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#454545" />
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.45" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Resources>
                    <ComboBox.Template>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid>
                                <ToggleButton x:Name="DropDownToggle"
                                              Background="{TemplateBinding Background}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="1"
                                              Foreground="{TemplateBinding Foreground}"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen,
                                                                  RelativeSource={RelativeSource TemplatedParent},
                                                                  Mode=TwoWay}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border x:Name="ToggleBorder"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4">
                                                <Grid>
                                                    <TextBlock Margin="10,0,30,0"
                                                               HorizontalAlignment="Left"
                                                               VerticalAlignment="Center"
                                                               Foreground="{TemplateBinding Foreground}"
                                                               Text="{Binding SelectedItem.Name,
                                                                              RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" />
                                                    <Path HorizontalAlignment="Right"
                                                          VerticalAlignment="Center"
                                                          Margin="0,0,10,0"
                                                          Data="M 0 0 L 4 4 L 8 0"
                                                          Stroke="#CCCCCC"
                                                          StrokeThickness="1.5" />
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ToggleBorder" Property="Background" Value="#80333333" />
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.45" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>

                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Fade"
                                       IsOpen="{TemplateBinding IsDropDownOpen}">
                                    <Border MinWidth="{Binding ActualWidth, ElementName=DropDownToggle}"
                                            MaxHeight="320"
                                            Margin="0,2,0,0"
                                            Padding="3"
                                            Background="#F21E1E1E"
                                            BorderBrush="#66FFFFFF"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                        <ScrollViewer>
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="DropDownToggle" Property="BorderBrush" Value="#99FFFFFF" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ComboBox.Template>
                    </ComboBox>
                </StackPanel>

                <Button x:Name="CloseButton"
                        Width="30" Height="30"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#CCCCCC"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="12"
                        Content="&#xE8BB;"
                        ToolTip="Close"
                        AutomationProperties.Name="Close AI answer"
                        Click="OnCloseClick">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border x:Name="CloseBorder"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CloseBorder" Property="Background" Value="#44FFFFFF" />
                                                <Setter Property="Foreground" Value="White" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="CloseBorder" Property="Background" Value="#66FFFFFF" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <TextBlock Grid.Row="1" Text="AI answer" FontSize="18" FontWeight="Bold"
                       Foreground="White" Margin="0,0,0,12"
                       Cursor="SizeAll"
                       ToolTip="Drag to move this window"
                       MouseLeftButtonDown="OnDragHandleMouseLeftButtonDown" />

            <Grid Grid.Row="2">
                <ScrollViewer x:Name="AnswerTextScrollViewer" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="AnswerText"
                               Text="Choose an AI skill, then press Enter to run."
                               TextWrapping="Wrap"
                               FontSize="15"
                               Foreground="#EEEEEE" />
                </ScrollViewer>
                <RichTextBox x:Name="AnswerMarkdownViewer"
                             Visibility="Collapsed"
                             IsReadOnly="True"
                             IsReadOnlyCaretVisible="True"
                             IsDocumentEnabled="True"
                             BorderThickness="0"
                             Padding="0"
                             Margin="0"
                             Background="Transparent"
                             Foreground="#EEEEEE"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled" />
            </Grid>

            <Grid Grid.Row="3" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Opacity" Foreground="#AAAAAA" FontSize="11"
                               VerticalAlignment="Center" Margin="0,0,8,0" />
                    <Slider x:Name="OpacitySlider" Width="120" VerticalAlignment="Center"
                            Minimum="0.2" Maximum="1.0"
                            SmallChange="0.05" LargeChange="0.1"
                            IsSnapToTickEnabled="True" TickFrequency="0.05"
                            ValueChanged="OnOpacityChanged" />
                </StackPanel>
                <TextBlock x:Name="InteractionHint" Text="Press Enter to run, or Esc to cancel"
                           Grid.Column="1"
                           Foreground="#AAAAAA" FontSize="11"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </Grid>
        </Border>

        <Thumb Style="{StaticResource ResizeThumbStyle}" Tag="Left"
               Width="8" HorizontalAlignment="Left" VerticalAlignment="Stretch"
               Cursor="SizeWE" DragDelta="OnResizeDragDelta" />
        <Thumb Style="{StaticResource ResizeThumbStyle}" Tag="Right"
               Width="8" HorizontalAlignment="Right" VerticalAlignment="Stretch"
               Cursor="SizeWE" DragDelta="OnResizeDragDelta" />
        <Thumb Style="{StaticResource ResizeThumbStyle}" Tag="Top"
               Height="8" HorizontalAlignment="Stretch" VerticalAlignment="Top"
               Cursor="SizeNS" DragDelta="OnResizeDragDelta" />
        <Thumb Style="{StaticResource ResizeThumbStyle}" Tag="Bottom"
               Height="8" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
               Cursor="SizeNS" DragDelta="OnResizeDragDelta" />

        <Thumb Style="{StaticResource ResizeThumbStyle}" Tag="TopLeft"
               Width="14" Height="14" HorizontalAlignment="Left" VerticalAlignment="Top"
               Cursor="SizeNWSE" DragDelta="OnResizeDragDelta" />
        <Thumb Style="{StaticResource ResizeThumbStyle}" Tag="TopRight"
               Width="14" Height="14" HorizontalAlignment="Right" VerticalAlignment="Top"
               Cursor="SizeNESW" DragDelta="OnResizeDragDelta" />
        <Thumb Style="{StaticResource ResizeThumbStyle}" Tag="BottomLeft"
               Width="14" Height="14" HorizontalAlignment="Left" VerticalAlignment="Bottom"
               Cursor="SizeNESW" DragDelta="OnResizeDragDelta" />
        <Thumb Style="{StaticResource ResizeThumbStyle}" Tag="BottomRight"
               Width="14" Height="14" HorizontalAlignment="Right" VerticalAlignment="Bottom"
               Cursor="SizeNWSE" DragDelta="OnResizeDragDelta" />
    </Grid>
</Window>
