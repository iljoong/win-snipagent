<Window x:Class="CaptureIt.App.Settings.AiTaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Task" Width="480" Height="560"
        MinWidth="420" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="OnCancelClick" />
            <Button Content="OK" Width="90" Click="OnOkClick" IsDefault="True" />
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Task Name:" Margin="0,0,0,2" />
                <TextBox x:Name="TaskNameTextBox" />
                <TextBlock x:Name="ValidationText" Foreground="Red" Margin="0,6,0,0" TextWrapping="Wrap" />

                <TextBlock Text="Task Prompt:" Margin="0,12,0,2" />
                <TextBox x:Name="TaskPromptTextBox" Height="130" AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" />

                <CheckBox x:Name="UseWebSearchCheckBox" Content="Use web search tool" Margin="0,14,0,0" />

                <DockPanel Margin="0,16,0,4">
                    <Button DockPanel.Dock="Right" x:Name="McpAddButton" Content="&#xE710;"
                            FontFamily="Segoe MDL2 Assets" Width="28" ToolTip="Add MCP server" Click="OnMcpAddClick" />
                    <TextBlock Text="MCP server:" VerticalAlignment="Center" />
                </DockPanel>
                <StackPanel x:Name="McpServersPanel" />
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
